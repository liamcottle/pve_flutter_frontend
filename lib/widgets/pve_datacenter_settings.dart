import 'package:flutter/material.dart';
import 'package:path/path.dart' as p;


class PveDatacenterSettings extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
          print(p.current);

    return Container(
    );
  }
}